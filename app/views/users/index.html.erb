<div class="row">
	<% @users.each do |user| %>
		<div class="col-xs-6 col-md-3">
			<%= link_to image_tag(user.image_link), user, class: :"thumbnail	" %>
			<h4><%= user.first_name %> <%= user.last_name %></h4>
		</div>
	<% end %>
</div>